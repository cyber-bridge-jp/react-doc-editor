import { Options } from './PollNode';
import { NodeKey } from 'lexical';

export default function PollComponent({ question, options, nodeKey, }: {
    nodeKey: NodeKey;
    options: Options;
    question: string;
}): JSX.Element;
//# sourceMappingURL=PollComponent.d.ts.map